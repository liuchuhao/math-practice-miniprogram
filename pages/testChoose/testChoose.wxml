<view class="container">
  <!-- 头部 -->
  <view class="header">
    <view class="header-icon">📚</view>
    <view class="title">选择年级</view>
    <view class="subtitle">西师版小学数学计算练习</view>
  </view>

  <!-- 快捷入口（上次选择） -->
  <view class="quick-entry" wx:if="{{lastGrade}}">
    <text class="quick-label">上次练习：</text>
    <view class="quick-btn" data-grade="{{lastGrade}}" bindtap="selectGrade">
      {{lastGrade}} →
    </view>
  </view>

  <!-- 年级列表 -->
  <view class="grade-list">
    <view 
      class="grade-item" 
      wx:for="{{gradeList}}" 
      wx:key="index" 
      data-grade="{{item}}" 
      bindtap="selectGrade"
      bindlongpress="showGradeDetail"
    >
      <view class="grade-icon">
        <text class="grade-num">{{index + 1}}</text>
      </view>
      
      <view class="grade-info">
        <view class="grade-header">
          <text class="grade-name">{{item}}数学</text>
          <text class="grade-difficulty">{{difficultyLevels[index]}}</text>
        </view>
        <text class="grade-desc">{{itemDescriptions[index]}}</text>
        
        <!-- 新增：题型标签 -->
        <view class="topic-tags">
          <text 
            class="topic-tag" 
            wx:for="{{gradeTopics[item]}}" 
            wx:for-item="topic"
            wx:key="name"
          >
            {{topic.icon}} {{topic.name}}
          </text>
        </view>
      </view>
      
      <view class="grade-arrow">
        <text class="arrow-icon">›</text>
      </view>
    </view>
  </view>

  <!-- 统计信息 -->
  <view class="stats-card">
    <view class="stat-item">
      <text class="stat-num">6</text>
      <text class="stat-label">年级</text>
    </view>
    <view class="stat-divider"></view>
    <view class="stat-item">
      <text class="stat-num">25+</text>
      <text class="stat-label">题型</text>
    </view>
    <view class="stat-divider"></view>
    <view class="stat-item">
      <text class="stat-num">∞</text>
      <text class="stat-label">练习题</text>
    </view>
  </view>

  <!-- 底部提示 -->
  <view class="footer">
    <text class="footer-text">💡 长按年级可查看详细题型</text>
  </view>
</view>
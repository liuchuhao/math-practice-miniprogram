/* pages/index/index.wxss */

/* 1. 容器：背景渐变 */
.container {
  padding: 30rpx;
  min-height: 100vh;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

/* 2. 头部文字 */
.header {
  text-align: center;
  margin-top: 10rpx;
  margin-bottom: 30rpx;
}

.title {
  font-size: 44rpx;
  font-weight: bold;
  color: #2c3e50;
  display: block;
  margin-bottom: 10rpx;
  text-shadow: 0 2rpx 4rpx rgba(0,0,0,0.1);
}

.subtitle {
  font-size: 28rpx;
  color: #7f8c8d;
}

/* 3. Logo区：旋转动画 */
.logo-area {
  display: flex;
  justify-content: center;
  margin-bottom: 40rpx;
}

.logo {
  width: 220rpx;
  height: 220rpx;
  background: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 10rpx 30rpx rgba(52, 152, 219, 0.2);
  position: relative;
  overflow: hidden;
}

.logo::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: conic-gradient(transparent, #3498db, transparent);
  animation: rotate 4s linear infinite;
}

@keyframes rotate {
  100% { transform: rotate(360deg); }
}

.logo-text {
  font-size: 100rpx;
  color: #3498db;
  font-weight: bold;
  font-family: 'Times New Roman', Times, serif;
  position: relative;
  z-index: 10;
  background: white;
  width: 200rpx;
  height: 200rpx;
  border-radius: 50%;
  text-align: center;
  line-height: 200rpx;
}

/* ==========================================
   4. 用户资料卡 (样式修复版)
   ========================================== */
   .user-card {
    background: rgba(255, 255, 255, 0.9);
    border-radius: 30rpx;
    margin-bottom: 40rpx;
    box-shadow: 0 10rpx 20rpx rgba(0,0,0,0.05);
    padding: 0; 
    overflow: visible; 
  }
  
  .user-card-inner {
    display: flex;
    align-items: center; /* 垂直居中 */
    padding: 40rpx;      /* 增加内边距，让整体居中一点 */
    gap: 40rpx;          /* 拉开头像和文字的距离 */
  }
  /* --- [新增] 头像包裹容器 --- */
/* 这是定位的基准 */
.avatar-wrap {
  position: relative;
  width: 130rpx;
  height: 130rpx;
  flex-shrink: 0;
  /* 确保不裁剪 */
  overflow: visible;
}
  /* --- 头像按钮核心样式 --- */
  .avatar-btn {
    padding: 0 !important;
  margin: 0 !important;
  background: none !important;
  line-height: normal !important;
  width: 100% !important; /* 填满包裹容器 */
  height: 100% !important;
  border-radius: 50%;
  position: relative;
  z-index: 1; /* 层级低 */
  }
  
  .avatar-btn::after { border: none; }
  
  /* 用户真实头像 */
  .avatar-img {
    width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 6rpx solid white;
  box-shadow: 0 5rpx 15rpx rgba(0,0,0,0.15);
  background-color: #eee;
  display: block; 
  }
  
  /* --- 相框叠加层样式 --- */
  .frame-overlay {
    position: absolute;
  /* 调整大小和位置，确保覆盖头像 */
  top: -16%;  
  left: -16%;
  width: 132%; 
  height: 132%;
  z-index: 10; /* 层级高，盖住头像 */
  pointer-events: none; /* 让点击穿透下去，可以点到下面的按钮换头像 */
  }
  
  /* 📷小图标 (那个绿色的东西) */
  .edit-badge {
    display: none;
  }
  
  /* --- 右侧信息栏 --- */
  .info-box {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  /* ... 后面的 nickname-row 等保持不变 ... */

/* 昵称行 */
.nickname-row {
  display: flex;
  align-items: center;
  border-bottom: 1rpx dashed #dcdcdc;
  padding-bottom: 10rpx;
  margin-bottom: 15rpx;
}

.nickname-input {
  font-size: 34rpx;
  font-weight: bold;
  color: #2c3e50;
  width: 100%;
}

.edit-icon {
  font-size: 24rpx;
  color: #bdc3c7;
  margin-left: 10rpx;
}

/* 积分军衔区 */
.rank-badge-area {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
}

.rank-pill {
  background: linear-gradient(90deg, #2c3e50, #4ca1af);
  color: #fff;
  font-size: 22rpx;
  padding: 6rpx 16rpx;
  border-radius: 10rpx;
  display: inline-flex;
  align-items: center;
  margin-bottom: 8rpx;
  box-shadow: 0 4rpx 8rpx rgba(44, 62, 80, 0.2);
}

.rank-icon {
  margin-right: 8rpx;
  font-size: 24rpx;
}

.rank-text {
  font-weight: bold;
  letter-spacing: 1rpx;
}

.integral-row {
  font-size: 24rpx;
  color: #7f8c8d;
  display: flex;
  align-items: baseline;
}

.integral-val {
  color: #e67e22;
  font-weight: bold;
  font-size: 28rpx;
  margin: 0 6rpx;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

.next-tips {
  color: #bdc3c7;
  font-size: 20rpx;
  margin-left: 8rpx;
}

/* 5. 核心功能区 (大卡片) */
.main-actions {
  display: flex;
  flex-direction: column;
  gap: 30rpx;
  margin-bottom: 40rpx;
}

.action-card {
  height: 160rpx;
  border-radius: 30rpx;
  display: flex;
  align-items: center;
  padding: 0 40rpx;
  color: white;
  position: relative;
  overflow: hidden;
  box-shadow: 0 10rpx 20rpx rgba(0,0,0,0.15);
  transition: transform 0.2s;
}

.action-card:active {
  transform: scale(0.98);
}

.action-card.blue {
  background: linear-gradient(135deg, #3498db, #2980b9);
  box-shadow: 0 8rpx 20rpx rgba(52, 152, 219, 0.4);
}

.action-card.orange {
  background: linear-gradient(135deg, #f39c12, #d35400);
  box-shadow: 0 8rpx 20rpx rgba(243, 156, 18, 0.4);
}

/* 新增紫色卡片 */
.action-card.purple {
  background: linear-gradient(135deg, #9b59b6, #8e44ad);
  box-shadow: 0 8rpx 20rpx rgba(155, 89, 182, 0.4);
}

.action-card::after {
  content: '';
  position: absolute;
  right: -30rpx;
  bottom: -50rpx;
  width: 180rpx;
  height: 180rpx;
  background: rgba(255,255,255,0.15);
  border-radius: 50%;
}

.icon-box {
  font-size: 60rpx;
  margin-right: 30rpx;
  width: 80rpx;
  text-align: center;
}

.text-box {
  display: flex;
  flex-direction: column;
}

.card-title {
  font-size: 36rpx;
  font-weight: bold;
}

.card-desc {
  font-size: 24rpx;
  opacity: 0.9;
  margin-top: 5rpx;
}

/* 6. 更多功能区 (网格小按钮) */
.sub-actions {
  display: grid;
  grid-template-columns: repeat(4, 1fr); /* 4列布局 */
  gap: 15rpx;
  margin-bottom: 40rpx;
}

.sub-btn {
  background: white;
  border-radius: 20rpx;
  height: 160rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  color: #555;
  box-shadow: 0 5rpx 15rpx rgba(0,0,0,0.05);
  transition: all 0.2s;
}

.sub-btn:active {
  background: #f8f9fa;
  transform: translateY(2rpx);
}

.sub-icon {
  font-size: 48rpx;
  margin-bottom: 15rpx;
}

/* 底部 */
.footer {
  margin-top: auto;
  opacity: 0.7;
  padding-bottom: 20rpx;
}

.footer-text {
  font-size: 24rpx;
  color: #7f8c8d;
  font-style: italic;
}